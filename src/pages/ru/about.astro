---
import StudioLayout from '../../layouts/StudioLayout.astro';
import About from '../../components/studio/About.astro';
import { getNewDirectionCopy } from '../../data/newDirectionI18n';

const lang = 'ru' as const;
const c = getNewDirectionCopy(lang);
---
<StudioLayout title={`overflow — ${c.layout.nav.about}`} description={c.about.lede} lang={lang} current="about">
  <About lang={lang} />
</StudioLayout>
