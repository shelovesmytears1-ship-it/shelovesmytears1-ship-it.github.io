---
import StudioLayout from '../../layouts/StudioLayout.astro';
import Contact from '../../components/studio/Contact.astro';
import { getNewDirectionCopy } from '../../data/newDirectionI18n';

const lang = 'ru' as const;
const c = getNewDirectionCopy(lang);
---
<StudioLayout title={`overflow — ${c.layout.nav.contact}`} description={c.contact.lede} lang={lang} current="contact">
  <Contact lang={lang} />
</StudioLayout>
