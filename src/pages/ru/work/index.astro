---
import StudioLayout from '../../../layouts/StudioLayout.astro';
import WorkIndex from '../../../components/studio/WorkIndex.astro';
import { getNewDirectionCopy } from '../../../data/newDirectionI18n';

const lang = 'ru' as const;
const c = getNewDirectionCopy(lang);
---
<StudioLayout title={`overflow — ${c.work.title} ${c.work.accent}`} description={c.work.lede} lang={lang} current="work">
  <WorkIndex lang={lang} />
</StudioLayout>
