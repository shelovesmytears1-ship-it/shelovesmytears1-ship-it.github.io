---
import StudioLayout from '../../layouts/StudioLayout.astro';
import Legal from '../../components/studio/Legal.astro';
import { getNewDirectionCopy } from '../../data/newDirectionI18n';

const lang = 'en' as const;
const c = getNewDirectionCopy(lang);
---
<StudioLayout title={`overflow — ${c.legal.impressumNav}`} description={c.legal.impressum.title} lang={lang}>
  <Legal lang={lang} doc="impressum" />
</StudioLayout>
