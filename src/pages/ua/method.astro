---
import StudioLayout from '../../layouts/StudioLayout.astro';
import Method from '../../components/studio/Method.astro';
import { getNewDirectionCopy } from '../../data/newDirectionI18n';

const lang = 'ua' as const;
const c = getNewDirectionCopy(lang);
---
<StudioLayout title={`overflow — ${c.method.title} ${c.method.accent}`} description={c.method.lede} lang={lang} current="method">
  <Method lang={lang} />
</StudioLayout>
